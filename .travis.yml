sudo: required
dist: trusty
before_install:
- sudo apt-get -qq update && sudo apt-get install -y --no-install-recommends texlive-xetex texlive-fonts-recommended
script: bash ./deploy.sh
env:
  global:
  - ENCRYPTION_LABEL: "7fafeaaa2ca4"
  - COMMIT_AUTHOR_EMAIL: "m_porter@live.co.uk"
